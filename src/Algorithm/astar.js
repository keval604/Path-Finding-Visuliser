
export const astar=(visitCtx,gridCtx)=>{
    const cor=gridCtx.cor; //terminal cordinate;    
    const noRows=gridCtx.noRows.rows;       //total no of rows and cols;
    const noCols=gridCtx.noCols.cols;

    // const visitCtx.visited //2d array 
    // visitCtx.setVisited(i,j,-3);
    function other(){

    }
    const executeAstar=()=>{
        
    }

    executeAstar();


}

// export default astar;

